# Docker Extension Compose File
# This file is used by Docker Desktop to run the extension backend only

services:
  penpot-extension:
    image: ${DESKTOP_PLUGIN_IMAGE}
    container_name: penpot-extension-backend
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
